{% extends 'base.html.twig' %}

{% block body %}
    <h1><span class="glyphicon glyphicon-tint" aria-hidden="true"></span>Nuevo Vehiculo</h1>
    
    {{ form_start(form) }}
    <div class="form-group col-md-12">
            {{ form_label(form.nombreVehiculo) }}
            {{ form_widget(form.nombreVehiculo) }}
            {{ form_errors(form.nombreVehiculo) }}
    </div>  
    <div class="form-group col-md-6">
        {{ form_label(form.lote) }}
        {{ form_widget(form.lote) }}
        {{ form_errors(form.lote) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_label(form.fechaVencimiento) }}
        {{ form_widget(form.fechaVencimiento) }}
        {{ form_errors(form.fechaVencimiento) }}
    </div>
    <div class="form-group col-md-6">
        {{ form_label(form.save) }}
        {{ form_end(form) }}
    </div>     
    <div class="form-group col-md-6">
        <a href="{{ path('medicamento_index') }}" class="btn btn-danger col-md-5">Cancelar</a>
    </div> 

{% endblock %}
