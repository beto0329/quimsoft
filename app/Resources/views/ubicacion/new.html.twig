{% extends 'base.html.twig' %}

{% block body %}
    <h1><span class="glyphicon glyphicon-map-marker" aria-hidden="true"></span>Nueva Ubicación.</h1>

    {{ form_start(form) }}
        <div class="form-group col-md-12">
            {{ form_label(form.ubicacion) }}
            {{ form_widget(form.ubicacion) }}
            {{ form_errors(form.ubicacion) }}
        </div>
        <hr>
        <div class="form-group col-md-6">
            {{ form_label(form.save) }}
            {{ form_end(form) }}
        </div>  
        <div class="form-group col-md-6">
            <a href="{{ path('guiaestabilidad_index') }}" class="btn btn-danger col-md-5">Cancelar</a>
        </div>  
{% endblock %}
